#include <stdio.h>
#include "main.h"

/**
 * _strlen - function that returns string length
 *
 * @s: string to return its length
 *
 * Return: nothing
 */
int _strlen(char *s)
{
	*s = str.length();
}
