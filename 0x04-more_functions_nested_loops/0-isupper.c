#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for character case
 *@c: The parameter being passed
 * Return: 1 for uppercase character and 0 for different character
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
	return (1);
}
{
	return (0);
}
}
